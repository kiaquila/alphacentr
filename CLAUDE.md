# CLAUDE.md — Web Project

@AGENTS.md

Use `AGENTS.md` as the instruction source of truth. Also read
`web-design.config.json` for the project's checks and payload budgets, and the
task-relevant product documents.
